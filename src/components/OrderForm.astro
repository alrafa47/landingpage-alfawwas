---
import packagesData from '../data/packages.json';
import site from '../data/site.json';
---

<section id="pesan" class="order-form">
  <div class="order-inner">
    <div class="section-header">
      <span class="badge badge-primary">Pemesanan Cepat</span>
      <h2 class="section-title">Order via WhatsApp</h2>
      <div class="section-divider">
        <span class="dot"></span>
        <span class="line"></span>
        <span class="dot"></span>
      </div>
      <p class="section-description">
        Isi formulir di bawah ini dan tim kami akan segera menghubungi Anda untuk konfirmasi pesanan.
      </p>
    </div>
    
    <div class="form-card">
      <form id="orderForm" class="form">
        <div class="form-row">
          <div class="form-group">
            <label class="form-label" for="parent_name">Nama Orang Tua</label>
            <input 
              type="text" 
              id="parent_name" 
              name="parent_name" 
              class="form-input" 
              placeholder="Contoh: Bpk. Ahmad"
              required
            />
          </div>
          <div class="form-group">
            <label class="form-label" for="child_name">Nama Anak</label>
            <input 
              type="text" 
              id="child_name" 
              name="child_name" 
              class="form-input" 
              placeholder="Contoh: Muhammad Yusuf"
              required
            />
          </div>
        </div>
        
        <div class="form-row">
          <div class="form-group">
            <label class="form-label" for="package">Pilihan Paket</label>
            <select id="package" name="package" class="form-select" required>
              <option value="">-- Pilih Paket --</option>
              {packagesData.packages.map((pkg) => (
                <option value={`${pkg.name} (${pkg.price})`}>Paket {pkg.name} - {pkg.price}</option>
              ))}
            </select>
          </div>
          <div class="form-group">
            <label class="form-label" for="event_date">Tanggal Acara</label>
            <input 
              type="date" 
              id="event_date" 
              name="event_date" 
              class="form-input"
              required
            />
          </div>
        </div>
        
        <div class="form-group">
          <label class="form-label" for="notes">Catatan Tambahan (Opsional)</label>
          <textarea 
            id="notes" 
            name="notes" 
            class="form-textarea" 
            placeholder="Tuliskan permintaan khusus Anda di sini..."
            rows="3"
          ></textarea>
        </div>
        
        <button type="submit" class="btn btn-whatsapp">
          <svg class="wa-svg" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z" fill="currentColor"/>
          </svg>
          Pesan via WhatsApp
        </button>
      </form>
      
      <p class="form-note">
        Pesan Anda akan otomatis terformat saat membuka aplikasi WhatsApp.
      </p>
    </div>
  </div>
</section>

<script define:vars={{ whatsapp: site.whatsapp }}>
  document.getElementById('orderForm').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const parentName = document.getElementById('parent_name').value;
    const childName = document.getElementById('child_name').value;
    const packageSelect = document.getElementById('package').value;
    const eventDate = document.getElementById('event_date').value;
    const notes = document.getElementById('notes').value;
    
    let message = `Assalamualaikum, saya ingin memesan Aqiqah:\n\n`;
    message += `*Nama Orang Tua:* ${parentName}\n`;
    message += `*Nama Anak:* ${childName}\n`;
    message += `*Paket:* ${packageSelect}\n`;
    message += `*Tanggal Acara:* ${eventDate}\n`;
    
    if (notes) {
      message += `*Catatan:* ${notes}\n`;
    }
    
    message += `\nTerima kasih.`;
    
    const encodedMessage = encodeURIComponent(message);
    window.open(`https://wa.me/${whatsapp}?text=${encodedMessage}`, '_blank');
  });
</script>

<style>
  .order-form {
    padding: 6rem 1.5rem;
    background: var(--color-section-alt);
  }
  
  @media (min-width: 1024px) {
    .order-form {
      padding: 6rem 10rem;
    }
  }
  
  .order-inner {
    max-width: 1280px;
    margin: 0 auto;
  }
  
  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .section-title {
    font-size: 1.875rem;
    color: var(--color-text);
    margin: 0.5rem 0 1rem;
  }
  
  @media (min-width: 1024px) {
    .section-title {
      font-size: 3rem;
    }
  }
  
  .section-description {
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 1rem auto 0;
  }
  
  .form-card {
    max-width: 700px;
    margin: 0 auto;
    background: white;
    border-radius: 1.5rem;
    border: 1px solid #e5e5e5;
    padding: 2rem;
    box-shadow: 0 25px 50px rgba(45, 70, 54, 0.05);
  }
  
  @media (min-width: 1024px) {
    .form-card {
      padding: 3rem;
    }
  }
  
  .form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  
  .form-row {
    display: grid;
    gap: 1.5rem;
  }
  
  @media (min-width: 768px) {
    .form-row {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  
  .form-label {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--color-text);
  }
  
  .form-input,
  .form-select,
  .form-textarea {
    width: 100%;
    padding: 1rem;
    font-size: 0.875rem;
    border: 1px solid #e5e5e5;
    border-radius: 0.75rem;
    background: white;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    font-family: inherit;
  }
  
  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(45, 70, 54, 0.1);
  }
  
  .btn-whatsapp {
    width: 100%;
    padding: 1.25rem;
    font-size: 1.125rem;
    border-radius: 0.75rem;
  }
  
  .wa-svg {
    width: 1.5rem;
    height: 1.5rem;
  }
  
  .form-note {
    text-align: center;
    font-size: 0.75rem;
    color: var(--color-text-secondary);
    margin-top: 1.5rem;
  }
</style>
